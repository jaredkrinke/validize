{
  "name": "validize",
  "version": "0.1.2",
  "description": "Tiny TypeScript library for validating JSON",
  "main": "validize.js",
  "scripts": {
    "build": "copy /y package.json dist && tsc -p .",
    "watch": "tsc -w -p .",
    "test": "mocha --require ts-node/register test/*.test.ts",
    "watch:test": "mocha --watch --require ts-node/register test/*.test.ts --watch --watch-extensions ts"
  },
  "author": "Jared Krinke",
  "license": "MIT",
  "files": [
    "validize.js",
    "validize.d.ts"
  ],
  "devDependencies": {
    "@types/assert": "^1.4.3",
    "@types/mocha": "^5.2.7",
    "assert": "^2.0.0",
    "mocha": "6.2.2",
    "ts-node": "^8.5.4",
    "typescript": "^3.7.4"
  },
  "dependencies": {}
}
